import Link from "next/link";
import Layout from "../components/Layout";

const IndexPage = () => (
	<Layout title="Home">
		<h1>Welcome to TypeScript + NextJS + GraphQL Auth Example</h1>
	</Layout>
);

export default IndexPage;
